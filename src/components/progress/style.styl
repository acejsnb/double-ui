.d-progress
  position absolute
  left 0
  top 0
  width 100%
  height 2px
  will-change transform
  animation-timing-function linear

@keyframes progressPlay
  from
    transform translateX(-50%) scaleX(0)
  to
    transform translateX(0) scaleX(1)

@keyframes progressReplay
  from
    transform translateX(-50%) scaleX(0)
  to
    transform translateX(0) scaleX(1)

.d-progress-play
  animation-play-state running
.d-progress-pause
  animation-play-state paused
  /*&::before
    position absolute
    left 0
    top 0
    content ''
    display block
    background-color #fff
    opacity .2
    width 100%
    height 100%
    will-change transform
    animation progressPlay 3s infinite*/
